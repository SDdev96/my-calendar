const LOCALHOST = "http://localhost";
const PORT = "8080";
const VERSION = "v1";
export const API_BASE_URL = `${LOCALHOST}:${PORT}/api/${VERSION}`;
export const EVENTS_SERVICE = `${API_BASE_URL}/events`;
